import React, { Component } from 'react';
import { View, Text} from 'react-native';
// import { Button, FormLabel, FormInput, FormValidationMessage } from 'react-native-elements';

export class Option extends Component {
  render() {
    return (
      <View>
        {/* <Button onPress={() => this.props.navigation.navigate('AddNoteScreen')} title="Add Note"/>
        <Button onPress={() => this.props.navigation.navigate('StartViewScreen')} title="Start View"/>
        <Button onPress={() => this.props.navigation.navigate('EditNoteScreen')} title="Edit Note"/> */}
      </View>
    )
  }
}

export default Option;